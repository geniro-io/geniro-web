import React, {
  createContext,
  useCallback,
  useContext,
  useEffect,
  useState,
} from 'react';

import { projectsApi } from '../api';
import type { ProjectDto } from '../autogenerated';

interface ProjectContextValue {
  projects: ProjectDto[];
  loading: boolean;
  loadProjects: () => Promise<void>;
}

export const ProjectContext = createContext<ProjectContextValue>({
  projects: [],
  loading: false,
  loadProjects: async () => {},
});

export const ProjectProvider: React.FC<{ children: React.ReactNode }> = ({
  children,
}) => {
  const [projects, setProjects] = useState<ProjectDto[]>([]);
  const [loading, setLoading] = useState(false);

  const loadProjects = useCallback(async () => {
    setLoading(true);
    try {
      const res = await projectsApi.getAllProjects();
      setProjects(res.data);
    } catch (err) {
      // Ignore on load failure — show empty state
      console.error('[ProjectContext] Failed to load projects:', err);
    } finally {
      setLoading(false);
    }
  }, []);

  useEffect(() => {
    void loadProjects();
  }, [loadProjects]);

  return (
    <ProjectContext.Provider value={{ projects, loading, loadProjects }}>
      {children}
    </ProjectContext.Provider>
  );
};

export const useProjectContext = () => useContext(ProjectContext);
